[project]
name = "mobile-sr-lab"
version = "0.1.0"
description = "Mobile-safe super-resolution model build and training tools"
requires-python = ">=3.10"
license = "Apache-2.0"

[tool.pytest.ini_options]
pythonpath = [".", "src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"
