name: Noise Remover purchase restriction validation
on:
  push:
    paths:
      - '.github/workflows/noise-remover-purchase-restriction.yml'
permissions:
  contents: read
env:
  APP_REPOSITORY: Persie0/noise_remover
  APP_REF: fix/purchase-not-allowed-info-only
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Validate private checkout token
        env:
          PRIVATE_TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN || secrets.GH_TOKEN }}
        run: test -n "$PRIVATE_TOKEN"
      - name: Checkout private app branch
        uses: actions/checkout@v5
        with:
          repository: ${{ env.APP_REPOSITORY }}
          ref: ${{ env.APP_REF }}
          token: ${{ secrets.PRIVATE_REPO_TOKEN || secrets.GH_TOKEN }}
          persist-credentials: false
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - name: Install dependencies
        run: flutter pub get
      - name: Show formatter diff
        run: |
          dart format lib/screens/settings_screen.dart test/purchase_restriction_reporting_test.dart
          git diff -- test/purchase_restriction_reporting_test.dart
      - name: Targeted tests
        run: flutter test test/purchase_restriction_reporting_test.dart
      - name: Strict analyze
        run: flutter analyze --fatal-infos
