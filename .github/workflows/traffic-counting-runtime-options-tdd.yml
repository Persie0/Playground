name: Traffic counting NCNN runtime options TDD

on:
  push:
    paths:
      - ".github/workflows/traffic-counting-runtime-options-tdd.yml"
      - "debug_triggers/traffic-counting-runtime-options-tdd.txt"
  workflow_dispatch:

permissions:
  contents: read

env:
  APP_REPOSITORY: Persie0/traffic-counting-light-main
  APP_REF: perf/pipeline-25fps

jobs:
  test:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Validate private checkout token
        env:
          PRIVATE_TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN || secrets.GH_TOKEN || secrets.GH_RELEASE_TOKEN }}
        run: test -n "$PRIVATE_TOKEN"
      - name: Checkout traffic-counting candidate
        uses: actions/checkout@v5
        with:
          repository: ${{ env.APP_REPOSITORY }}
          ref: ${{ env.APP_REF }}
          token: ${{ secrets.PRIVATE_REPO_TOKEN || secrets.GH_TOKEN || secrets.GH_RELEASE_TOKEN }}
          persist-credentials: false
          path: app
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: app/edge/pyproject.toml
      - name: Install edge test dependencies
        working-directory: app/edge
        run: |
          python -m pip install --upgrade pip
          python -m pip install ".[dev]" ncnn
      - name: Run NCNN runtime option test
        working-directory: app/edge
        run: PYTHONPATH=src pytest -q tests/test_ncnn_runtime_options.py
