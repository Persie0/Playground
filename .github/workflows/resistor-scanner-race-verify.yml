name: Resistor scanner race verification

on:
  push:
    paths:
      - '.github/workflows/resistor-scanner-race-verify.yml'

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout Playground for result
        uses: actions/checkout@v5
        with:
          path: report

      - name: Validate private checkout token
        env:
          PRIVATE_TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN || secrets.GH_TOKEN }}
        run: test -n "$PRIVATE_TOKEN"

      - name: Checkout app revision
        uses: actions/checkout@v5
        with:
          repository: Persie0/resistor_scanner
          ref: fix/glitchtip-34-camera-race
          token: ${{ secrets.PRIVATE_REPO_TOKEN || secrets.GH_TOKEN }}
          persist-credentials: false
          path: app

      - name: Checkout private CI harness
        uses: actions/checkout@v5
        with:
          repository: Persie0/resistor_scanner
          ref: tflite
          token: ${{ secrets.PRIVATE_REPO_TOKEN || secrets.GH_TOKEN }}
          persist-credentials: false
          path: private-ci
          sparse-checkout: .github/playground
          sparse-checkout-cone-mode: false

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - uses: gradle/actions/setup-gradle@v5
        with:
          gradle-version: '9.6.0'

      - name: Run private scanner/camera suite
        id: scanner
        continue-on-error: true
        shell: bash
        run: |
          set +e
          bash private-ci/.github/playground/scanning-tests.sh app 2>&1 | tee scanner.log
          status=${PIPESTATUS[0]}
          echo "exit_code=$status" >> "$GITHUB_OUTPUT"
          exit "$status"

      - name: Record verification result
        working-directory: report
        env:
          OUTCOME: ${{ steps.scanner.outcome }}
          EXIT_CODE: ${{ steps.scanner.outputs.exit_code }}
        run: |
          mkdir -p results
          APP_SHA="$(git -C ../app rev-parse HEAD)"
          printf 'outcome=%s\nexit_code=%s\napp_sha=%s\n' "$OUTCOME" "$EXIT_CODE" "$APP_SHA" > results/resistor-scanner-race-verify.txt
          cp ../scanner.log results/resistor-scanner-race-verify.log
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add results/resistor-scanner-race-verify.txt results/resistor-scanner-race-verify.log
          git commit -m "ci: record resistor scanner race verification"
          git push origin HEAD:main

      - name: Preserve failing status
        if: steps.scanner.outcome != 'success'
        run: exit 1
