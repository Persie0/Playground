name: Traffic analysis quality fixes
# Temporary Superpowers TDD harness for the vehicle-scale/sneckdown implementation. Task 7 replay GREEN verification retry.

on:
  push:
    branches:
      - traffic-analysis-quality-fixes
  workflow_dispatch:

permissions:
  contents: read

env:
  TARGET_REPOSITORY: Persie0/traffic-counting-light-main
  TARGET_REF: feature/traffic-analysis-quality-fixes

jobs:
  focused-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout Playground
        uses: actions/checkout@v4

      - name: Validate private repository token
        env:
          PRIVATE_TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN || secrets.GH_TOKEN }}
        run: |
          if [[ -z "$PRIVATE_TOKEN" ]]; then
            echo "::error::PRIVATE_REPO_TOKEN or GH_TOKEN is required."
            exit 2
          fi

      - name: Checkout traffic branch
        uses: actions/checkout@v4
        with:
          repository: ${{ env.TARGET_REPOSITORY }}
          ref: ${{ env.TARGET_REF }}
          token: ${{ secrets.PRIVATE_REPO_TOKEN || secrets.GH_TOKEN }}
          path: target
          lfs: false

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install lightweight test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install 'numpy>=2.2,<2.5' 'opencv-python-headless>=4.12,<5' 'pytest>=9,<10' 'ruff>=0.16,<0.17' 'easydict>=1.13,<2' 'loguru>=0.7.3,<0.8' 'pyyaml>=6,<7'

      - name: Compile
        working-directory: target
        run: python -m compileall -q edge/src

      - name: Check Task 1-7 undefined names
        working-directory: target/edge
        run: |
          python -m ruff check --select F \
            src/analysis/traffic/vehicle_scale.py \
            src/analysis/traffic/heatmap_state.py \
            src/analysis/traffic/waiting_detector.py \
            src/analysis/traffic/occupied_area.py \
            src/analysis/traffic/renderer.py \
            src/analysis/traffic/traffic_stage.py \
            src/analysis/traffic/storage.py \
            src/utils/config.py \
            scripts/replay_traffic_heatmaps.py \
            src/analysis/analysis_pipeline.py \
            tests/test_vehicle_scale.py \
            tests/test_waiting_detector.py \
            tests/test_occupied_area.py \
            tests/test_traffic_renderer.py \
            tests/test_analysis_pipeline.py \
            tests/test_traffic_stage.py \
            tests/test_heatmap_storage.py \
            tests/test_config.py \
            tests/test_traffic_replay.py

      - name: Run Task 1-7 tests
        working-directory: target/edge
        run: python -m pytest -q tests/test_vehicle_scale.py tests/test_waiting_detector.py tests/test_occupied_area.py tests/test_traffic_renderer.py tests/test_analysis_pipeline.py tests/test_traffic_stage.py tests/test_heatmap_storage.py tests/test_config.py tests/test_traffic_replay.py
